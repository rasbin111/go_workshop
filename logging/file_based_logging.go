package main

/*
import (
	"log"
	"os"
)

var (
	WarningLog *log.Logger
	InfoLog    *log.Logger
	ErrorLog   *log.Logger
)

func init() {
	file, err := os.OpenFile("logs.txt", os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0666)

	if err != nil {
		log.Fatal(err)
	}

	InfoLog = log.New(file, "INFO: ", log.LstdFlags|log.Lshortfile)
	WarningLog = log.New(file, "WARNING: ", log.LstdFlags|log.Lshortfile)
	ErrorLog = log.New(file, "ERROR: ", log.LstdFlags|log.Lshortfile)
}

func main() {
	// file, err := os.OpenFile("logs.txt", os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0666)
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// log.SetOutput(file)
	// log.Println("Welcome to file based logging in Golang.")

	InfoLog.Println("Starting the application...")
	InfoLog.Println("Something has occurred...")
	WarningLog.Println("WARNING!!!")
	ErrorLog.Println("Some error has occurred...")

}
*/
